{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "src/strategies/**",
      "src/core/**",
      "src/services/**",
      "src/api/**",
      "src/workers/**",
      "src/main.py",
      "src/mcp_trading_server.py",
      "requirements.txt",
      "Dockerfile",
      "railway.json"
    ]
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  },
  "services": {
    "OniQuant-Gateway": {
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile",
        "watchPatterns": [
          "src/strategies/**",
          "src/core/**",
          "src/services/**",
          "src/api/**",
          "src/main.py",
          "requirements.txt",
          "Dockerfile"
        ]
      },
      "deploy": {
        "startCommand": "uvicorn src.main:app --host 0.0.0.0 --port $PORT",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 10,
        "numReplicas": 1,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
      },
      "envs": {
        "POSTGRES_URL": { "$default": "${{shared.POSTGRES_URL}}" },
        "REDIS_URL": { "$default": "${{shared.REDIS_URL}}" }
      }
    },
    "OniQuant-Safety-Worker": {
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile",
        "watchPatterns": [
          "src/strategies/**",
          "src/core/**",
          "src/services/**",
          "src/workers/trailing_stop.py",
          "requirements.txt",
          "Dockerfile"
        ]
      },
      "deploy": {
        "startCommand": "celery -A src.workers.trailing_stop worker --beat --loglevel=info",
        "numReplicas": 1,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
      },
      "envs": {
        "POSTGRES_URL": { "$default": "${{shared.POSTGRES_URL}}" },
        "REDIS_URL": { "$default": "${{shared.REDIS_URL}}" }
      }
    },
    "OniQuant-Recursive-Optimizer": {
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile",
        "watchPatterns": [
          "src/core/**",
          "src/services/**",
          "src/workers/optimizer_job.py",
          "requirements.txt",
          "Dockerfile"
        ]
      },
      "deploy": {
        "startCommand": "celery -A src.workers.optimizer_job worker --beat --loglevel=info",
        "numReplicas": 1,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
      },
      "envs": {
        "POSTGRES_URL": { "$default": "${{shared.POSTGRES_URL}}" },
        "REDIS_URL": { "$default": "${{shared.REDIS_URL}}" }
      }
    },
    "OniQuant-MCP-Analyst": {
      "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "Dockerfile",
        "watchPatterns": [
          "src/strategies/**",
          "src/core/**",
          "src/services/**",
          "src/mcp_trading_server.py",
          "requirements.txt",
          "Dockerfile"
        ]
      },
      "deploy": {
        "startCommand": "python src/mcp_trading_server.py",
        "numReplicas": 1,
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 5
      },
      "envs": {
        "POSTGRES_URL": { "$default": "${{shared.POSTGRES_URL}}" },
        "REDIS_URL": { "$default": "${{shared.REDIS_URL}}" }
      }
    }
  }
}
